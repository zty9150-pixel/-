1，abc254c	对于Ai可以与Ai+k交换位置，观察每一个Ai,Ai+k,Ai+2k子序列是否可以排为上升序列去解决问题

2，abc346d	对于a[1]或第一行可以决定这个数组或二维数组，进行题目升级，用dp，前缀和，后缀和做，关键是将选择两个相同的相邻数转化为将两个相邻数两端的代价算出来，用前缀和与后缀和快速计算，最后枚举这两个数来找到最小代价
			变式1	  二维数组，每次可翻十字形区域01，全翻到1/0最少多少步。枚举第一行2**n次方种可能，接下来所有行跟着上一行走。最后来到最后一行，保证全为0/1即可得到全翻0/1ans，最后找到最小ans。

3，洛谷p1969 / cf1392c	  堆积木 可以选定一个区间同时堆+1，只要加上下一个比i高的max(a[i+1] - a[i],0)即可得出最后需要多少步堆出积木
			变式1          cf1392f 选区间+1，要变成递增数组只计算往下一个低的堆出下一个更高的积木需要的代价即可得出步数
			变式2	  选区间+1或-1，把数组变0，分别计算增数的代价与减数的代价最后统一加上即可得出
			变式3	  允许选区间[1,x]或[x,n]+1,堆积木堆到目标数组，看差分数组，对高处用+1计入ans，低处在高处+1同时-1，ans = （h1​+hn​+∑∣hi​−hi+1​∣）/2

4，力扣134	环形路加油站，选择起始点保证全程不断油。贪心做法，前缀和折线图，找到前缀和最小的折线点，此点下一个位置即为出发点。

5，单调队列	动态求得区间的最值。push：o(n),push的元素永远比左手元素大/小。pop：o1,如果要pop队列最值元素只要pop最左边的，不是最值则不管。最值查看：o1,函数只要查看最左边的元素。单调队列尽量储存元素在原数组下标而非元素值。

6，堆		push：o(logn),建堆则是o(nlogn)。pop：o(logn)。top：o1。

7，递归复杂度	主要看每一层干了多少活。二分：共logn层，每层o1，共logn。归并：共logn层，每层走n(1层n，二层n/2+n/2，三层n/3+n/3+n/3)，共nlongn。快排：nlogn。快速选择：共logn层，但每层只走一半，1+1/2+1/4······共2n，复杂度o(n)

8，约束传播abc469d		通过一个小区间的约束推广到大区间，去约束大区间，得出答案。要保证答案（x,y）至少有一个出现在每一组组合过，得到约束条件第一组要么A要么B是答案x/y。要么x=A1要么x=B1，得到所有答案区间，再逐个验证即可。

9，ST表		状态dp与倍增思想的结合，st[i][j]，i是区间左端点，2^j是区间长度，用于求解静态区间最值问题

10，树状数组	特点是动态单点修改 + 动态前缀和查询。所有操作都是logn。也引出了区间和，后缀和。多应用于逆序对，动态排名。注：树状数组不能涉及下标0。

11，离散化	快速离散化：1，将原数组丢进新vector<int> b	2，对b排序	3,对b去重		4，查询新数值：lower_bound() - b.begin() + 1。

12，相邻交换排序模型	本质：消除所有逆序对。逆序对：归并，树状，线段树

13，树状数组变化	动态单点修改 + 动态前缀和查询 ----->>> 动态单点查询 + 动态区间修改：	树状数组作为差分数组，l-r加d，即tree[l]+d,tree[r+1]-d,这样每一个点的值就变成了a[i]+前缀和tree[1-i]

14，abc470c	注意点1，异或相同的元素会变成0，异或0会不变。注意点2，这题的时间复杂度，遍历一个大于0的数组最后时间复杂度不会是N*Q，因为操作2遍历数组次数小于操作1，总的会小于2Q，最后是N+Q。











未解决：堆，单调栈，cdq分治，快速幂，哈希表记录最长不连续字符串字符最后一次出现位置